1°com duas variáveis, fiz a soma, subtração, divisão e multiplicação

2°somei as três variáveis, após isso dividi elas por 3

3°com duas variáveis, fiz a entrada como "A" e atribui "B" em "(A*9/5)+32"

4°com duas variáveis, usei B*H e com isso resultou na área do retângulo 

5°com uma variável, utilizei "A-1" e "A+1"

6°com uma variável, fiz "A*2" e "A/2"

7°com duas variáveis, sendo "A" as horas trabalhadas no mês e "B" sendo o valor da hora, e com isso fiz A*B oq definiu o salario final

8°com duas variáveis, fiz duas entradas "A,B" e como saída fiz "A/B"

9°com três variáveis, fiz entrada "A,B" e atribui "C" como "(B*A/100)" e como saída somei "A+C"

10°com duas variáveis, e fiz duas entradas "A" e outra como "B" após apliquei a saída e dividi "A/B"  
